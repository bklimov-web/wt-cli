# wt — план переезда на Go

Контекст решений зафиксирован в grilling-сессии (2026-09-11). Кратко: текущий
`wt` — рабочий bash-черновик для создания git worktree (`new`, `ls`, `rm`,
`open`, `path`). Переписываем на Go, потому что bash тяжело читать и
дополнять, а Go даёт единый бинарник без рантайма — полезно и сейчас, и если
дойдём до публичного релиза.

Работаем фазами. Фаза A — личный инструмент. Фаза B — публичный релиз,
начинается только если A прижилась на практике.

---

## Фаза A — личный инструмент на Go

Цель: `wt`, которым реально пользуюсь каждый день на рабочем проекте, с
полным паритетом фич + начальные улучшения. Bash-версия выводится из
использования сразу, без параллельного проживания.

### A0 — Каркас проекта
- [x] `go mod init` с плейсхолдер module path (сменить одной командой
      `go mod edit -module` при публикации)
- [x] Выбрать и подключить `urfave/cli` для команд
- [x] Выбрать и подключить `huh` для интерактивного пикера
- [x] Выбрать TOML-библиотеку (`BurntSushi/toml` или `pelletier/go-toml`)
- [x] Базовая структура: `main.go` + пакеты (`internal/config`,
      `internal/worktree`, `internal/editor`, ...) — решить границы пакетов
      по ходу, не обязательно фиксировать заранее

> 📘 **Учебная заметка:** структура Go CLI обычно: `main.go` тонкий (парсинг
> флагов, вызов пакетов), вся логика — в `internal/`. Это отличается от
> bash, где всё в одном файле — компенсируем модульностью, которой в bash не
> было.

### A1 — Конфигурация
- [ ] Схема конфига: расположение worktree-папки, паттерн ветки, команда
      редактора, список env-файлов для копирования, install-команда/override
- [ ] Чтение глобального `~/.config/wt/config.toml`
- [ ] Чтение опционального per-repo `.wt.toml` (оверрайд глобальных значений)
- [ ] Слияние: дефолты → global config → per-repo config → env-переменные
      (`WT_ENV_FILES`, `WT_NO_INSTALL`, `WT_NO_CODE` остаются как разовый
      оверрайд поверх всего)
- [ ] Дефолты совпадают с текущим bash-поведением (`../<repo>-wt`,
      `feature/<name>`, `code`, auto-detect пакетного менеджера)

### A2 — Команда `new`
- [ ] Резолв main checkout + определение default branch (порт логики)
- [ ] `git worktree add` от `origin/<default>`
- [ ] Копирование env-файлов
- [ ] Auto-detect + запуск install (pnpm/yarn/bun/npm по lock-файлу)
- [ ] Открытие в редакторе (если не отключено)
- [ ] Решить: что делать при падении install на середине (сейчас bash просто
      падает, worktree уже создан) — rollback или оставить как есть с
      понятным сообщением?

### A3 — Команда `ls`
- [ ] Порт вывода (branch + path), можно улучшить форматирование раз уж
      переписываем

### A4 — Команда `rm`
- [ ] Интерактивный пикер через `huh`, если имя не передано
- [ ] **Новое:** подтверждение перед удалением worktree + branch
      (деструктивная операция, в bash-версии подтверждения не было)

### A5 — Команда `open`
- [ ] Порт логики (включая пикер с пунктом "main")
- [ ] Известный баг: зависание VS Code при `wt open` существующего
      worktree — не чиним в рамках порта, фиксируем как отдельную задачу
      ниже

### A6 — Команда `path`
- [ ] Тривиальный порт

### A7 — Обкатка
- [ ] Пользоваться Go-версией на реальном рабочем проекте несколько дней
- [ ] Собрать список того, что раздражает/не хватает
- [ ] Решить: фаза A считается успешной / стоит ли идти в фазу B

---

## Отдельно, не блокирует фазу A

- [ ] Задебажить зависание VS Code на `wt open` (нужно сначала
      воспроизвести и понять: конфликт процессов? `code` не в фоне?
      indexing?)

---

## Фаза B — публичный релиз (стартует только если A прижилась)

- [ ] Довести конфиг до реального generic-состояния, задокументировать все
      поля
- [ ] Написать README с разделом про конфликт имени `wt` с другими
      бинарниками на PATH (как переименовать/сделать alias)
- [ ] Решить механизм дистрибуции: npm / homebrew tap / `go install` /
      curl-скрипт с GitHub Releases
- [ ] Решить module path и опубликовать репозиторий
- [ ] Agent skill: анализирует конкретный репозиторий пользователя и
      помогает сгенерировать/настроить `.wt.toml` под него — часть
      инструкции по установке
- [ ] Лицензия

---

## Отклонённые/отложенные идеи

- **Полный отказ от скрипта в пользу ad hoc агента на каждый вызов** —
  отклонено: операция частая и рутинная, хочется секунд, а не ожидания
  агента.
- **Поддержка редакторов кроме VS Code** — отложено до фазы B, но команда
  редактора уже конфигурируема с фазы A, так что добавить будет дёшево.
- **Переименование бинарника ради избежания коллизий** — отклонено, `wt`
  остаётся; конфликт решается на стороне пользователя (см. README в фазе B).
